// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

//Update schema: npx prisma migrate dev --name <CamelCaseIstruction> es. <CreateUserTable> 
//Open prisma studio: npx prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum Role {
  ADMIN
  USER
}

model User {
  id  Int @id @default(autoincrement())
  name String
  email String @unique
  role Role @default(USER)
  password String
  address Address[] //User hava multiple address
  createdAt DateTime @default(now())
  updateAt DateTime @default(now())

  @@map("user")
}

model Product {
  id Int @id @default(autoincrement())
  name String
  description String
  price Decimal
  tags String
  createdAt DateTime @default(now())
  updateAt DateTime @default(now())

  @@map("products")
}

model Address {
  id Int @id @default(autoincrement())
  lineOne String
  lineTwo String?
  city String
  country String
  pincode String
  userId Int
  user User @relation(fields: [userId], references: [id]) //Address has one user
  createdAt DateTime @default(now())
  updateAt DateTime @default(now())

  @@map("address")
}